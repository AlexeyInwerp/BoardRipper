# macOS
.DS_Store

# Node
src/frontend/node_modules/
src/frontend/dist/
src/frontend/dist-ssr/
src/frontend/.vite/

# Test results
src/frontend/test-results/
src/frontend/playwright-report/

# Go
src/backend/server
src/backend/boardripper
src/backend/boardripper-server

# Docker build artifacts
data/

# Local Docker Compose files and overrides
compose.*.y*ml
docker-compose.*.y*ml
# …except the checked-in dev container. RELEASE_RUNBOOK tells you to run it and
# scripts/devcontainer.sh reads it, so a fresh clone must have it; it holds no
# local paths or credentials (library = ./samples, data = ./.devdata).
!docker-compose.dev.yml

# Local scratch/dev runtime overrides
scratch/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# Desktop (Electron) build artifacts
desktop/node_modules/
desktop/out/
desktop/out-legacy/
desktop/out-win/
desktop/webapp/
desktop/bin/

# Samples — ignored by default. The public repo does NOT ship real-world
# boardview files or schematic PDFs (not redistributable). Local working tree
# may have whatever you bring. To re-introduce a redistributable test fixture,
# whitelist it explicitly here.
samples/*

# Local dev-container state (docker-compose.dev.yml mounts this as /data).
# Delete it for a cold-start install; never commit it.
.devdata/

# Claude — local settings stay out of the public repo
.claude/settings.json
.claude/worktrees/
.worktrees/

# Internal AI-assisted-development working notes — not for public consumption
docs/superpowers/
docs/agents/
docs/analysis/

# SQLite WAL / shared-memory transient files (created when DB is open)
*.db-shm
*.db-wal

# Test results (root level)
test-results/

# Deploy credentials
deploy.conf
*.tar.gz

# Local release artifacts (use GitHub Releases for distribution)
release-artifacts/

# Release pipeline staging — generated by scripts/release.sh
out/

# Claude scheduled-tasks lockfile
.claude/scheduled_tasks.lock

# Transient import-review staging files; never committed
import-staging/
**/__pycache__/
.tavily-env
.superpowers/
