/**
 * Altium PCB ASCII Version 5.0 variant — placeholder.
 *
 * T16 fills the real implementation. This stub exists so altium-pcb-parser
 * can import it without the file being absent.
 */
import type { BoardData } from '../types';

export function parseAltiumAscii(_buffer: ArrayBuffer): BoardData {
  throw new Error('Altium PCB ASCII variant not yet implemented (Task 16).');
}
