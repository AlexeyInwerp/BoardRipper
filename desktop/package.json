{
  "name": "boardripper",
  "version": "1.0.0",
  "description": "BoardRipper — standalone desktop app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "node build-mac.mjs",
    "build:all": "node build-all.mjs",
    "build:mac": "node build-all.mjs --mac",
    "build:legacy": "node build-all.mjs --legacy",
    "build:win": "node build-all.mjs --win"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "@electron/universal": "^3.0.2",
    "electron": "^35.1.5"
  }
}
